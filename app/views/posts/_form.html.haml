= form_for @post do |f|
  - if @post.errors.any?
    #error_explanation
      %h2
        = @post.errors.count
        件のエラーが発生したため保存ができませんでした。
      %ul
        - @post.errors.full_messages.each do |msg|
          %li= msg
  %p
    = f.label :title, "タイトル"
    %br/
    = f.text_field :title
  %p
    = f.label :content, "内容"
    %br/
    = f.text_area :content, cols: 60, rows: 8
  %p
    = f.submit
