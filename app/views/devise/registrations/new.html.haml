.contents
  .timage
    = image_tag "Dipressd_man.png", size:'100x100'

  %h2 新規登録
  %hr{width:"25%"}/

  = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
    = render "devise/shared/error_messages", resource: resource
    .field
      .field__circle-image
        = f.label :アイコン
        = f.label :必須, class:'hissu'
        %br/
        - if @user.image.present?
          = image_tag @user.image.url, id: 'img_prev'
        - else
          = image_tag "sample.jpg", id:'img_prev'
        = f.file_field :image, class: 'field__dropzone', id: "user_img"
        %br
        %hr{width:"50%"}/

      = f.label :ニックネーム
      = f.label :必須, class:'hissu'
      %em (最大20文字)
      %br/
      = f.text_field :nickname, autofocus: true, maxlength: "20", class: 'input-title'
      %hr{width:"50%"}/

    .field
      = f.label :メールアドレス
      = f.label :必須, class:'hissu'
      %br/
      = f.email_field :email, autofocus: true, autocomplete: "email", class: 'input-title'
      %hr{width:"50%"}/

    .field
      = f.label :パスワード
      = f.label :必須, class:'hissu'
      - if @minimum_password_length
        %em
          (#{@minimum_password_length} 文字以上)
      %br/
      = f.password_field :password, autocomplete: "new-password", class: 'input-title'
      %hr{width:"50%"}/

    .field
      = f.label :パスワード（確認用）
      = f.label :必須, class:'hissu'
      %br/
      = f.password_field :password_confirmation, autocomplete: "new-password",  class: 'input-title'
      %br/
      %hr{width:"50%"}/

    .actions
      = f.submit "アカウント作成", class: 'post'
      %hr{width:"25%"}/
      = link_to '戻る', root_path, class: 'post'
      %hr{width:"25%"}/

  = render "devise/shared/links"

  .timage
    = image_tag "Dipressd_man.png", size:'100x100'